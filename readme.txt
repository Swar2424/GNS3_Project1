à ne pas oublier : 

de manière générale:
- vérifier que les interfaces des routeurs dans GNS3 correspondent aux interfaces renseignées dans l'intent file

pour le Telnet:
- bien vérifier les ports des routeurs
- ouvir la console de chaque routeur et appuyer sur "Entrée" pour l'initialiser 